import "./logo.css";
const Logo = () => {
  return (
    <span className="font-alexandria text-5xl font-bold text-primary-blue">
      Fin<span className="logo-gradient">Track.</span>
    </span>
  );
};

export default Logo;
